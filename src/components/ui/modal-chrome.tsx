/**
 * Shared Radix Dialog chrome — "Modernist Organic" panel + overlay treatment.
 * Compose with `cn()` alongside positioning classes in each dialog.
 */
export const overlayClass = "fixed inset-0 z-50 bg-zinc-950/50 backdrop-blur-sm";

export const contentClass =
  "rounded-3xl border border-zinc-200/80 bg-white p-0 shadow-lg dark:border-white/10 dark:bg-zinc-900";

export const sheetContentClass =
  "fixed inset-y-0 right-0 z-50 flex w-full max-w-md flex-col border-l border-zinc-200/80 bg-white shadow-lg dark:border-white/10 dark:bg-zinc-900";

export const closeButtonClass =
  "inline-flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-foreground-muted hover:bg-zinc-100 dark:hover:bg-white/10";