/**
 * Shared Radix Dialog chrome — "Modernist Organic" panel + overlay treatment.
 * Compose with `cn()` alongside positioning classes in each dialog.
 */
export const overlayClass = "fixed inset-0 z-50 bg-black/55 backdrop-blur-sm";

export const contentClass =
  "rounded-3xl border border-border bg-surface p-0 shadow-lg";

export const sheetContentClass =
  "fixed inset-y-0 right-0 z-50 flex w-full max-w-md flex-col border-l border-border bg-surface shadow-lg";

export const closeButtonClass =
  "inline-flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-foreground-muted hover:bg-surface-muted";