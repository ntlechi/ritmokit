/**
 * Legacy export — Training Portal LMS is the canonical catalog UI.
 * Kept so existing imports keep working.
 */
export { TrainingPortal as SopCatalog } from "@/components/training/TrainingPortal";
